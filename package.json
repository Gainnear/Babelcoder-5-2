{
  "name": "unit5-2",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "0.6.1"
  },
  "dependencies": {
    "react": "^15.3.2",
    "react-dom": "^15.3.2",
    "react-router": "^2.8.1"
  },
  "scripts": {
    "start": "concurrently --kill-others \"npm run start-dev\" \"npm run start-api\"",
    "start-dev": "react-scripts start",
    "start-api": "json-server --watch db.json --port 8080",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
